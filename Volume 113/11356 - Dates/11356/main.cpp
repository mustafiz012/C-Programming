#include <bits/stdc++.h>

using namespace std;

int main()
{
    int T, y, d, k;
    char song[12][100] = {"January", "February", "March", "April", "May", "June", "July", "August", "September", "October", "November", "December"};
    char dated[6];
    scanf("%d", &T);
    while(T--){
            int date = 0;
        while(strlen(dated)--){
            cin>>y;
            date++;
            scanf("%s", name);
            date++;
            cin>>d;
            date++;
        }
        cin>>k;

    }
    return 0;
}
