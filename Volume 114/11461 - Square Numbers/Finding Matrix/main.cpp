#include <iostream>
#include<cstdio>

using namespace std;

int main()
{
    int T, N, matrix[101][101];
    cin>>T;
    while(T--){
        cin>>N;
        for(int i=0; i<N; i++)
            for(int j=0; j<N; j++)
                cin>>matrix[i][j];
        for(int i=0; i<N; i++)
            for(int j=0; j<N; j++){

            }
    return 0;
}
