#include<bits/stdc++.h>
#include<cstdio>
using namespace std;

int main()
{
    int T, a, b, sum;
    cin >> T;
    while(T != 0)
    {
        sum = 0;
        cin >> a >> b;
        sum = 0;
        scanf("%d%d", &n, &m);
        sum = (a/3)*(b/3);
        printf("%lld\n", sum);
        T--;
    }
}
